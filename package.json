{
  "name": "agent-reputation-oracle",
  "version": "1.0.0",
  "description": "Decentralized reputation tracking for agents based on task performance and peer reviews",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
